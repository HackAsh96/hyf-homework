import React from 'react';
import ReactDOM from 'react-dom';
import CompsWrapper from './CompsWrapper';

import './styles.css';

const rootElement = document.getElementById('root');
ReactDOM.render(<CompsWrapper />, rootElement);
